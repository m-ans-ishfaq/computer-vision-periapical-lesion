# Periapical Lesion Analysis

Classification, detection, and segmentation of periapical lesions in panoramic dental X-rays.

## Git workflow
- `src/` — source code (config, models, training, utils)
- `notebooks/` — Jupyter notebooks
- `data/` — excluded from git (on Google Drive)
- `outputs/` — excluded (generated at runtime)
